# Week2 keypoint-2

## definiton of limit

    We usually think limit is just a situation. But, it is false. Limit is a contant. 
    We define f(x)->L, as x->a like these.
     1. Make open interval except a (a-r,a),(a,a+r)
     2. compress r to 0.
     3. analyze all f(x) in the interval when compressing r to 0.
     4. find the number when compressing. it is L. and it is constant.
     
